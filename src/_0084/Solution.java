package _0084;

import java.util.concurrent.ConcurrentHashMap;

public class Solution {
    public int largestRectangleArea(int[] heights) {
        int left = 0;
        int right = 0;
        while (left != heights.length || right != heights.length - 1) {

        }
        return 0;
    }
}
